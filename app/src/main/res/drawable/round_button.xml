<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Background color of the button -->
    <solid android:color="@color/AccountBgButton" />
    <!-- Round corners -->
    <corners android:radius="50dp" />
    <!-- Optional: Add border (e.g., black border) -->
    <stroke android:color="@color/AccountBgButton" android:width="0dp" />
</shape>
